# Common Example Notebook Inventory

The common track should own notebooks that demonstrate shared surfaces instead of backend internals.

Current notebook categories:

- common Topo adapter usage
- common SMPL adapter usage
- common NumPy runtime usage
- shared diagnostics and artifact export flows

Notebook guidance is standardized in `../standards/example_notebook_standard.md`.
