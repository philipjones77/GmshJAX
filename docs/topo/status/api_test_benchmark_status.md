# Topo API, Test, Benchmark, And Example Status

Current state:

- Topo exposes direct and common-adapter API surfaces
- workflow and export coverage exists across modes 1 through 5
- retained benchmark harnesses exist for modes 1 through 5
- benchmark harness validation is under pytest control
- Topo examples cover initialization, optimization, adaptive workflows, diagnostics, and export

Current documentation and coverage gaps to watch:

- keep reports synchronized as new benchmark or example entry points are added
- keep the practical docs aligned with the current common adapter surface as it evolves
- expand notation and theory only when it clarifies real workflow behavior
